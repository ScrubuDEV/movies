/* tslint:disable:no-unused-variable */

import { TruncateDecimalPipe } from './truncate-decimal.pipe';

describe('Pipe: TurnicateFloate', () => {
  it('create an instance', () => {
    let pipe = new TruncateDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
